import React from "react";
import MarkdownComponent from "react-native-markdown-package";

export const Markdown = ({ children }: any) => {
  return <MarkdownComponent>{children}</MarkdownComponent>;
};
